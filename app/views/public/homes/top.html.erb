<div class="container">
    <div class="row mt-5">
        <div class="col-xl-3">
        <table class="table table-bordered genre-table">
            <thead>
                <td>ジャンル検索</td>
            </thead>
            <tbody>
                <% @genres.each do |genre| %>
                <tr>
                    <td><%= link_to genre.name, search_path('search[value]': genre.id, 'search[how]': "match") %></td>
                </tr>
                <% end %>
            </tbody>
        </table>
        </div>
        <div class="col-xl-9">
            <div class="w-autoz h-50 text-center pt-5">
                <div class="top-im">
                    <h3>ようこそ、ながのCAKEへ！</h3></br>
                    <p>このサイトは、ケーキ販売にECサイトになります。</br>
                    会員でない方も商品の閲覧はできますが、</br>
                    購入には会員登録が必要になります。
                    </p>
                    <% if flash[:notice] %>
                        <%= flash[:notice] %>
                    <% end %>
                </div>
            </div>
            <div class="mt-5">
                <h1>新着商品</h1>
                <div class="rrtt">
                    <% @products.each do |product| %>
                        <%= link_to product_path(product.id) do %>
                            <div class="image"><%= attachment_image_tag product, :image, size: "200x200", fallback:"no_image.png" %></div>
                            <div class="name"><%= product.name %></div>
                            <div class="price">¥<%= number_to_currency(product.price,:precision =>0, :format=>"%n%u", :unit=>"円") %>（税抜き）</div>
                        <% end %>
                    <% end %>
                </div>
                <%= link_to "全ての商品を見る>", products_path %>
            </div>
        </div>
    </div>
</div>

<style>
.rrtt {
  display: flex;
  flex-wrap: wrap;
}

.image {
    margin-right: 20px;
    margin-bottom: px;
}

.name {
    margin-bottom: 10px;
}

.price {
    margin-bottom: 50px;
}

.background-image {
    background-image: image-url("forest.jpeg");
    background: red;
    height: 350px;
}
</style>